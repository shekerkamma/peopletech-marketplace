---
name: patterns
description: "EMBEDDED — Discovers recurring patterns across analyses. This functionality is built into the run-analysis skill. Do NOT invoke patterns separately — use run-analysis instead, which includes these instructions inline."
---

# patterns

This skill's core functionality has been absorbed into **run-analysis**.

When run-analysis triggers, it automatically handles what patterns used to do.

Do not invoke this skill directly. Use run-analysis instead.
