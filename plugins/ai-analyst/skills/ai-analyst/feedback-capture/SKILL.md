---
name: feedback-capture
description: "EMBEDDED — Detects corrections and learnings from user messages. This functionality is built into the ask-question skill. Do NOT invoke feedback-capture separately — use ask-question instead, which includes these instructions inline."
---

# feedback-capture

This skill's core functionality has been absorbed into **ask-question**.

When ask-question triggers, it automatically handles what feedback-capture used to do.

Do not invoke this skill directly. Use ask-question instead.
