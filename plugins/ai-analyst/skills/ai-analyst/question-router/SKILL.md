---
name: question-router
description: "EMBEDDED — Classifies question complexity (L1-L5) and routes to appropriate response path. This functionality is built into the ask-question skill. Do NOT invoke question-router separately — use ask-question instead, which includes these instructions inline."
---

# question-router

This skill's core functionality has been absorbed into **ask-question**.

When ask-question triggers, it automatically handles what question-router used to do.

Do not invoke this skill directly. Use ask-question instead.
