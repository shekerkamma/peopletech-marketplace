{
  "manifest_version": "0.3",
  "name": "firecrawl-pp-mcp",
  "display_name": "Firecrawl",
  "version": "3.6.0",
  "description": "Firecrawl API surface as MCP tools.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/firecrawl-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/firecrawl-pp-mcp",
      "args": [],
      "env": {
        "FIRECRAWL_BEARER_AUTH": "${user_config.firecrawl_bearer_auth}"
      }
    }
  },
  "user_config": {
    "firecrawl_bearer_auth": {
      "type": "string",
      "title": "FIRECRAWL_BEARER_AUTH",
      "description": "Sets FIRECRAWL_BEARER_AUTH for the Firecrawl MCP server.",
      "sensitive": true,
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
